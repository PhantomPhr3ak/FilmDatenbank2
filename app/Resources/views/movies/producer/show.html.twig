{% extends 'base.html.twig' %}

{% block body %}
    <h1>Producer</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ producer.name }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ producer.website }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_producer_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_producer_edit', { 'id': producer.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
